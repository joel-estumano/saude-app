import { TextActionSubmitPipe } from './text-action-submit.pipe';

describe('TextActionSubmitPipe', () => {
	it('create an instance', () => {
		const pipe = new TextActionSubmitPipe();
		expect(pipe).toBeTruthy();
	});
});
